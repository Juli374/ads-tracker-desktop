// CalendarBell — keep export for back-compat, body promoted to CalendarDropdown
// (mini-month grid + AddEvent). Old simple list-only Bell stayed read-only;
// new CalendarDropdown is a richer surface with month nav and event creation.
export { CalendarDropdown as CalendarBell } from './CalendarDropdown';
